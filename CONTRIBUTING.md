# 贡献指南

感谢参与 Open AI Canvas。提交改动前，请先确认相关 Issue 或用简短说明描述问题、目标和外部行为变化。

## 开发环境

- 前端：Bun、Vite、React、TypeScript。
- 后端：Go、Gin、GORM、PostgreSQL。
- 本地全栈：`docker compose up -d --build`。

不要提交 `.env`、数据库、数据目录、生成产物、编辑器配置或真实 API/OSS 密钥。新增配置项时只在 `.env.example` 中提供无敏感信息的说明。

## 提交要求

- 保持改动聚焦，沿用现有目录职责、命名和错误处理风格。
- 写路径必须明确失败；不可用默认值掩盖保存、权限、生成或删除错误。
- 后端对象读取必须同时校验当前用户和资源归属。
- 涉及上传、外部请求或模型调用时，必须说明大小、频率、费用和 SSRF 边界。
- 用户可见文案使用中文；核心业务入口和安全边界使用简短中文注释说明原因。

## 面向上游的最小功能 PR

- 功能通常先在内部业务分支完成并验证；确认上游也需要后，再同步上游并从 `upstream/main` 新建独立的 `feat/*` 或 `fix/*` 分支，将可公开的最小实现迁入。不要直接从长期演进的内部业务分支提 PR。
- 一个 PR 只解决一个 Issue 或独立能力。提交前以 `upstream/main...HEAD` 检查差异，保留实现、必要测试和直接相关文档，排除内部功能、部署改动、发布记录、敏感配置及无关历史。
- 需要部署配置的能力必须默认未配置，由部署方显式提供。尤其是方舟相关代码，严禁提交 AK/SK、Endpoint、Region、Ark ProjectName、素材组 ID 或任何生产配置、示例默认值和数据库记录。
- 上游 PR 的抽取不改变内部 `custom/main` 作为业务与部署主线；抽取时遇到结构冲突，保留内部业务、权限和品牌约定，逐块迁移所需能力，不整段合并长期分叉的历史。
- PR 描述应说明对应 Issue、改动摘要、风险、验证方式和必要截图；未执行验证时必须如实标注。

Pull Request 应包含改动摘要、风险、验证方式和必要截图。提交即表示你有权按本项目 AGPL-3.0 许可证贡献相关代码或素材，并保留已有上游署名和许可证通知。
