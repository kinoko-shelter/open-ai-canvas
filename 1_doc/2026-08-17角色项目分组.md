# 概述
1. 角色 现在是固定值,先不改
2. 创建 部门表 aigc_dept, 参考mars_kol里的 sys_depte 表结构,注意 通过 parent_id 组成树形结构
3. 创建 项目表 aigc_project (project_id, project_name(项目名), project_name_outer(对外项目名), project_type(项目类型), project_desc(项目说明),status(启用,禁用,用汉字), 级别(一级,二级)
4. users表,加字段 dept_id, 关联到 

* 注意: 下面的代码都放到 backend/aigc,  本项目的 backend 是来自开源项目,我们再此开源项目上进行改造,那么 对文件夹明显的隔离,将有利于将来的合并新代码feature
* 注意命名:用 AigcXxxx 这样命名
* 权限控制: 管理员后台逻辑不变, 不运行 管理员 意外的任何角色进入看看到菜单
* 新增: 运营管理后台 页面, 放置 项目管理 ,允许,管理员, 团队主管 角色进入


aigc_dept  aigc_project 表一般都有这几个字段:
remark 备注
created_at 创建日期
updated_at 最后更新日期

created_by 创建人
updated_by 最后更新人

注意,下面说的 sys_ 开头的表都属于 mars_kol

# 用户表改造
1. users表,加字段 dept_id, 关联到 aigc_dept

# 角色改造
增加角色: 运营管理, 团队主管,团队成员

# 角色表
暂无,避免改的太多,先按现在结构

# 项目配置：
项目表 aigc_project 实现增删改查
部门管理 实现增删改查
添加用户,编辑用户信息的页面,增加 请选择部门(只能选二级部门及以下)


#  页面变化
管理员后台 - 平台资源 下的用户管理菜单 上面 增加: 部门管理(再分左侧树形结构,右侧列表,顶部筛选项,添加按钮等)
运营管理后台: 按现有结构增加左侧菜单,  增加: 项目管理


项目表上加个 dept_id ,默认0, 团队主管创建项目时,默认先读这个团队主管的用户的 dept_id 作为项目项目的dept_id
项目页面, 团队主管可以看到. dept_id 等于0(一级项目都是0) 的,或者 dept_id 和自己相同的dept_id 的项目

团队主管只可以修改自己的项目
管理员可以看到和修改任何全部项目